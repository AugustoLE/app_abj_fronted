import 'package:flutter/material.dart';

class MathGame2Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Matemáticas Juego 2')),
      body: Center(child: Text('Ventana de ejercicio Matemáticas 2')),
    );
  }
}