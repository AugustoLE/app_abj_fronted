import 'package:flutter/material.dart';

class CommGame1Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Comunicación Juego 1')),
      body: Center(child: Text('Ventana de ejercicio Comunicación 1')),
    );
  }
}