import 'package:flutter/material.dart';

class MathGame1Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Matemáticas Juego 1')),
      body: Center(child: Text('Ventana de ejercicio Matemáticas 1')),
    );
  }
}
