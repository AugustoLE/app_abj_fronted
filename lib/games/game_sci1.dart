import 'package:flutter/material.dart';

class SciGame1Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Ciencia Juego 1')),
      body: Center(child: Text('Ventana de ejercicio Ciencia 1')),
    );
  }
}