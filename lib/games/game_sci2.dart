import 'package:flutter/material.dart';

class SciGame2Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Ciencia Juego 2')),
      body: Center(child: Text('Ventana de ejercicio Ciencia 2')),
    );
  }
}